<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit</title>
    <link rel="stylesheet" href="/css/style.css">

  </head>
  <body>
    <%- include('../partials/_navbar.ejs') %>
    <h1>Edit My Book </h1>
    <a href="/books/<%=book._id%>" class="back-link" >&larr; Go Back</a>
    <div class="information-container">
      <div class="book-text">
        <form action="/books/<%= book._id %>?_method=PUT"  method="POST">
            <div class="form-group">
              <label for="name"> Book Title: </label>
              <input  type="text" id="name" name="name" value="<%= book.name%>" readonly>
            </div>
            <div class="form-group">
              <label for="name"> Author: </label>
              <input  type="text" id="name" name="author" value="<%= book.author%>" readonly>
            </div>
            <div class="form-group">
              <label for="desription">Description: </label>
              <textarea  id="description" name="description" rows="5" maxlength="500" required><%=book.description%></textarea>
            </div>
            <div class="form-group">
              <label for="genre"> Literary Genre: </label>
              <input list="genres" id="genre" name="genre" value="<%= book.genre %>" placeholder="Select or type a genre" required>
                <datalist id="genres">
                  <option value="Fantasy">
                  <option value="Science Fiction">
                  <option value="Mystery">
                  <option value="Thriller">
                  <option value="Romance">
                  <option value="Historical Fiction">
                  <option value="Horror">
                  <option value="Adventure">
                  <option value="Biography">
                  <option value="Memoir">
                  <option value="Self-Help">
                  <option value="History">
                  <option value="Science">
                  <option value="Travel">
                  <option value="Essay">
                  <option value="Philosophy">
                  <option value="Poetry">
                  <option value="Drama">
                  <option value="Graphic Novel">
                </datalist>
            </div>
            <div class="form-group">
              <label for="pages"> Number of Pages: </label>
              <input  type="number" id="pages" name="pages" min="0" value="<%= book.pages %>"required>
            </div>
            <button type="submit" class="submitbtn">Update</button>
        </form>
      </div>
      <div class="book-image">
        <img class="book-cover" src="<%= book.coverURL %>" alt="Cover for <%= book.name %>">
      </div>
    </div>  
  <%- include('../partials/footer') %>
  </body>
</html>

